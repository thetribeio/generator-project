services:
    <%= packageName %>:
        build: <%= packageName %>/docker
        volumes:
            - ./<%= packageName %>:/usr/src/project/<%= packageName %>
        environment:
            PORT: 8080
        ports:
            - 8080:8080
