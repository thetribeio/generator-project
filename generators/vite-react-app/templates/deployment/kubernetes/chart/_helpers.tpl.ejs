{{- define "<%= projectName %>.<%= packageName %>.image" }}
{{- .Values.registry }}/<%= projectName %>-<%= packageName %>
{{- if .Values.<%= varName(packageName) %>.image.digest -}}
@{{ .Values.<%= varName(packageName) %>.image.digest }}
{{- else -}}
:{{ .Values.<%= varName(packageName) %>.image.tag }}
{{- end -}}
{{- end }}
