server {
    listen 80;

    root /srv/<%= packageName %>;

    client_max_body_size 25M;

    location / {
        try_files $uri /index.html;

        add_header cache-control "no-store";
    }

    location /assets/ {
        try_files $uri =404;

        add_header cache-control "public, max-age=31536000, immutable";
    }
}
