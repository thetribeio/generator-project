- import_role:
    name: postgresql
  vars:
    dbs:
      <%= varName(packageName) %>:
        owner: <%= varName(packageName) %>
    users:
      <%= varName(packageName) %>:
        password: '{{ database_<%= varName(packageName) %>_password }}'
  when: database_host == "localhost"
