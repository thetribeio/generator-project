- import_role:
    name: postgresql
  vars:
    dbs:
      <%= varify(packageName) %>:
        owner: <%= varify(packageName) %>
    users:
      <%= varify(packageName) %>:
        password: '{{ database_<%= varify(packageName) %>_password }}'
  when: database_host == "localhost"
