- import_role:
    name: postgresql
  vars:
    dbs:
      <%= packageName.replace('-', '_') %>:
        owner: <%= packageName.replace('-', '_') %>
    users:
      <%= packageName.replace('-', '_') %>:
        password: '{{ <%= packageName.replace('-', '_') %>_database_password }}'
