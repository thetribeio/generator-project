<%= varName(packageName) %>_port: 4000
<%= varName(packageName) %>_env:
  COOKIE_SECRET: '{{ <%= varName(packageName) %>_cookie_secret }}'
  DATABASE_URL: postgres://<%= varName(packageName) %>:{{ database_<%= varName(packageName) %>_password }}@{{ database_host }}/<%= varName(packageName) %>
  ENVIRONMENT_NAME: '{{ environment_name }}'
  NODE_ENV: production
  PORT: '{{ <%= varName(packageName) %>_port }}'
  SENTRY_DSN: ~ # TODO: Add your sentry DSN here
