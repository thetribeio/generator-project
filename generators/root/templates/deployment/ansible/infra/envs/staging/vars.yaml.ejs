branch: develop
contact_email: <%= contactEmail %>
domain: <%= domain %>
environment_name: staging
basic_auth:
  user: <%= projectName %>
  password: !vault |
<%= indent(encrypt(basicAuthPassword), 4) %>
