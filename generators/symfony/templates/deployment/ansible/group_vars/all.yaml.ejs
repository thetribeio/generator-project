<%= varName(packageName) %>_env:
  DATABASE_URL: postgresql://<%= varName(packageName) %>:{{ database_<%= varName(packageName) %>_password }}@{{ database_host }}/<%= varName(packageName) %>
  ENVIRONMENT_NAME: '{{ environment_name }}'
  APP_ENV: prod
  APP_SECRET: '{{ <%= varName(packageName) %>_secret }}'
  SENTRY_DSN: ~ # TODO: Add your sentry DSN here
<%= varName(packageName) %>_php_version: 8.1
