version: '2.4'

services:
    <%= packageName %>-node:
        build:
            context: <%= packagePath %>/docker/node
            args:
                UID: ${uid:-1000}
        command: yarn start
        depends_on:
            - <%= packageName %>-nginx
        volumes:
            - ./<%= packagePath %>:/usr/src/project/<%= packagePath %>
