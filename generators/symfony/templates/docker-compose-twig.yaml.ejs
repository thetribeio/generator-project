version: '2.4'

services:
    <%= packageName %>-nginx:
        build: <%= packagePath %>/docker/nginx
        depends_on:
            - <%= packageName %>-php
        volumes_from:
            - <%= packageName %>-php

    <%= packageName %>-node:
        build:
            context: <%= packagePath %>/docker/node
            args:
                UID: ${UID:-1000}
        command: yarn start
        depends_on:
            - <%= packageName %>-nginx
        volumes_from:
            - <%= packageName %>-php
